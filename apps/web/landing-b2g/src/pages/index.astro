---
/**
 * Landing page principal: gobierno.altrupets.app
 * Uses Landing layout (minimal header + urgency bar).
 * Sections: UrgencyBar (in layout), Hero, Mantra 1, Problems, ForWho,
 * Mantra 2, Process, SocialProof, VideoSection, FAQ, FinalCTA, Footer (in layout).
 */
import Landing from "@/layouts/Landing.astro";
import Hero from "@/components/Hero.astro";
import MantraBanner from "@/components/MantraBanner.astro";
import Problems from "@/components/Problems.astro";
import ForWho from "@/components/ForWho.astro";
import Process from "@/components/Process.astro";
import SocialProof from "@/components/SocialProof.astro";
import VideoSection from "@/components/VideoSection.astro";
import FAQ from "@/components/FAQ.astro";
import FinalCTA from "@/components/FinalCTA.astro";
---

<Landing
  title="AltruPets — Gesti&oacute;n de Bienestar Animal para Municipalidades"
  meta_title="AltruPets — Gesti&oacute;n de Bienestar Animal para Municipalidades"
  description="Cumpla con la Ley 7451 sin depender de SENASA. Panel de control de bienestar animal para su cant&oacute;n."
>
  <!-- S-02: Hero -->
  <Hero />

  <!-- S-03: Mantra 1 -->
  <MantraBanner
    text="Si no puede medir cu&aacute;ntos animales rescat&oacute; su cant&oacute;n este mes, no puede justificar el presupuesto del pr&oacute;ximo."
    bgColor="bg-secondary-90"
    textColor="text-primary"
  />

  <!-- S-04: Problems (three problems, three solutions) -->
  <Problems />

  <!-- S-05: ForWho ("AltruPets es para su municipio si...") -->
  <ForWho />

  <!-- S-06: Mantra 2 -->
  <MantraBanner
    text="Los datos no mienten. Pero para tener datos, primero necesita un sistema."
    bgColor="bg-primary-20"
    textColor="text-white"
  />

  <!-- S-07: Process (3 steps) -->
  <Process />

  <!-- S-08: SocialProof (impact numbers) -->
  <SocialProof />

  <!-- S-09: VideoSection (video embed + CTAs) -->
  <VideoSection />

  <!-- S-10: FAQ (6 questions) -->
  <FAQ />

  <!-- S-11: FinalCTA -->
  <FinalCTA />
</Landing>
