# 🏗️ Arquitectura - Visión General

## Arquitectura Híbrida

AltruPets utiliza una **arquitectía híbrida** que combina un monorepo bien organizado con servicios especializados:

```
┌─────────────────────────────────────────────────────────────────┐
│                    Presentation Layer                             │
│                   Flutter App (Web/Mobile)                        │
└──────────────────────────┬──────────────────────────────────────┘
                           │ GraphQL
┌──────────────────────────▼──────────────────────────────────────┐
│                      API Layer                                    │
│               GraphQL API (Backend NestJS)                        │
└──────────────────────────┬──────────────────────────────────────┘
                           │ REST + Database
┌──────────────────────────▼──────────────────────────────────────┐
│                    Backend Layer                                 │
│          NestJS + PostgreSQL + Valkey                            │
└──────────────────────────┬──────────────────────────────────────┘
                           │ Infrastructure
┌──────────────────────────▼──────────────────────────────────────┐
│                  Infrastructure Layer                             │
│              OVHCloud Kubernetes + Terraform                      │
└─────────────────────────────────────────────────────────────────┘
```

## Principios Arquitectónicos

### 1. Clean Architecture (Flutter)

```
lib/
├── core/                    # Compartido entre features
│   ├── payments/        # Paquete de pagos LATAM
│   ├── network/          # Cliente HTTP
│   └── services/         # Servicios globales
├── features/             # Features del dominio
│   ├── auth/
│   ├── pets/
│   ├── rescues/
│   ├── adoptions/
│   └── donations/
└── shared/               # Utilidades comunes
```

### 2. Modular Monolith (Backend)

```
backend/
├── modules/
│   ├── auth/
│   ├── users/
│   ├── pets/
│   ├── rescues/
│   ├── adoptions/
│   └── donations/
└── shared/
```

### 3. Monorepo Compartido

- **Skills de IA**: `/skills`
- **Configuración MCP**: `mcp.json`
- **Tokens de diseño**: `design-tokens/`
- **Infraestructura**: `infrastructure/`

## 🛡️ Seguridad

- **JWT Authentication** con refresh tokens
- **RBAC** (Role-Based Access Control)
- **Rate Limiting** por endpoint
- **Tokenización PCI** para pagos
