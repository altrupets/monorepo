apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: backend
          image: altrupets-backend:dev
          imagePullPolicy: Never
          env:
            - name: NODE_ENV
              value: "development"
